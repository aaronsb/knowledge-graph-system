# ============================================================================
# Knowledge Graph System - Docker Compose (GHCR Images)
# ============================================================================
# Use pre-built images from GitHub Container Registry instead of building locally
#
# Usage:
#   docker-compose -f docker-compose.yml -f docker-compose.ghcr.yml up -d
#
# This override file replaces local builds with published container images.
# ============================================================================

services:
  postgres:
    image: ghcr.io/aaronsb/knowledge-graph-system/kg-postgres:latest

  api:
    # ADR-101: KG_API_IMAGE_TAG selects ROCm variants (rocm60/rocm61/rocm72-host)
    # for AMD hosts. Operator sets it from GPU_MODE; unset means CPU/NVIDIA
    # which use the default kg-api:latest (PyPI torch wheel ships CUDA runtime).
    image: ghcr.io/aaronsb/knowledge-graph-system/kg-api:${KG_API_IMAGE_TAG:-latest}

  web:
    image: ghcr.io/aaronsb/knowledge-graph-system/kg-web:latest

  operator:
    image: ghcr.io/aaronsb/knowledge-graph-system/kg-operator:latest
