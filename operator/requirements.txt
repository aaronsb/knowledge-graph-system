# Operator minimal dependencies
# For database configuration and credential management
# Last updated: 2026-09-17

# Database access
psycopg2-binary==2.9.13

# Password hashing (for admin user creation)
bcrypt==5.0.0

# Encryption (for API key storage via api.api.lib.encrypted_keys)
cryptography==50.0.1

# Python dotenv (for reading .env files in diagnostics)
python-dotenv==1.2.3

# AI provider SDKs (for API key validation and catalog refresh in configure.py)
# openai 3.x / anthropic 1.x use httpx2 (the Pydantic-maintained fork) and no
# longer depend on httpx; httpx is kept pinned only for other transitive users.
httpx==0.28.1
openai==3.14.1
anthropic==1.6.0
requests>=2.31.0  # For model catalog refresh (OpenRouter, Ollama)

# Numeric computation (for grounding calculation in admin scripts)
# 2.5.x requires Python >= 3.12 — see the 3.13-slim base in operator/Dockerfile
numpy==2.5.3
