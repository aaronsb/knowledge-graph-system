# Knowledge Graph Visualizer - Environment Configuration
# Copy this file to .env and update values for your environment

# =============================================================================
# API Configuration
# =============================================================================

# API server base URL
# Development: http://localhost:8000
# Production: https://api.kg.example.com
VITE_API_URL=http://localhost:8000

# =============================================================================
# OAuth 2.0 Configuration
# =============================================================================

# OAuth client ID (must match a client registered in the database)
# Default builtin client: kg-viz (configured in migration 022)
VITE_OAUTH_CLIENT_ID=kg-viz

# OAuth redirect URI (must match client's redirect_uris in database)
# Development: http://localhost:3000/callback
# Production: https://viz.kg.example.com/callback
# Note: If not set, will use window.location.origin + '/callback'
# VITE_OAUTH_REDIRECT_URI=http://localhost:3000/callback

# =============================================================================
# Application Configuration
# =============================================================================

# App name displayed in UI
VITE_APP_NAME=Knowledge Graph Visualizer

# App version (optional, for display purposes)
# VITE_APP_VERSION=1.0.0

# =============================================================================
# Configuration Precedence
# =============================================================================
#
# The app checks configuration in this order:
# 1. Runtime config (public/config.js - window.APP_CONFIG) - HIGHEST PRIORITY
# 2. Build-time env vars (.env - import.meta.env.VITE_*) - fallback
# 3. Hardcoded defaults - last resort
#
# For development:
#   - Use public/config.js (already configured for localhost)
#   - OR use .env (this file) if you prefer environment variables
#
# For Docker deployment:
#   - docker-entrypoint.sh generates config.js from environment variables
#   - No .env file needed in container
#
# For CDN deployment (Netlify, Vercel, Cloudflare Pages):
#   - Build once with: npm run build
#   - Create production config.js in dist/ folder
#   - Deploy dist/ to CDN
#   - See DEPLOYMENT.md for full instructions

# =============================================================================
# Development Setup
# =============================================================================
#
# 1. Copy this file to .env: cp .env.example .env
# 2. Start API server: cd .. && ./scripts/start-api.sh
# 3. Start vite: npm run dev
# 4. Open browser: http://localhost:3000
