// Runtime Configuration for Knowledge Graph Visualizer
// This file is loaded at runtime, allowing CDN deployments without rebuilding
//
// For CDN deployment (Netlify, Vercel, Cloudflare Pages, S3+CloudFront):
// 1. Copy this file to config.js
// 2. Update values for your environment
// 3. Deploy both the built app AND config.js to your CDN
//
// For Docker deployment:
// - This file is automatically generated from environment variables at startup

window.APP_CONFIG = {
  // API server base URL
  apiUrl: 'http://localhost:8000',

  // OAuth 2.0 configuration
  oauth: {
    clientId: 'kg-viz',
    // Redirect URI - if null, uses window.location.origin + '/callback'
    redirectUri: null, // or 'https://viz.example.com/callback'
  },

  // Application metadata
  app: {
    name: 'Knowledge Graph Visualizer',
    version: '1.0.0',
  },
};
